const { describe, it } = require("node:test");
const { deepStrictEqual, strictEqual } = require("assert");
const { Buffer } = require("../src/buffer");

describe("Buffer", () => {
  describe("storeText", () => {
    it("should store the text in the buffer", () => {

    });
  });
});